.DS_Store
node_modules/
*.log
.cache
.direnv
.nitro
.output
.wrangler
dist
app.config.timestamp_*.js
.env
.env*.local
.claude/worktrees
.claude/*.lock
storybook-static
coverage
/result*

# turbo
.turbo

# vitest screenshot/attachment artifacts
.vitest-attachments
**/__screenshots__

# Panda CSS codegen output. Deterministic from `panda.config.ts` and
# scanned source — regenerated by the root `prepare` script on install.
/packages/lib/styled-system/src/
