## Storybook test harness

Set up `@storybook/test` for interactive story-level testing.
Use `fn()` spies for event handlers and `play` functions for
assertions. Support headless execution via `storybook test`.
