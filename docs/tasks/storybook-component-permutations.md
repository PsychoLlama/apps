## Storybook component permutations

Explore ways with Storybook to view permutations of a component at once
using knobs. The goal is seeing every variant/state combination on a
single page instead of toggling one knob at a time.
