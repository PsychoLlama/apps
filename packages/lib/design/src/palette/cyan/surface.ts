/** Translucent panel-ish background, distinct from `cyan[2]`. Light mode. */
export const light = '#eff9facc';

/** Translucent panel-ish background, distinct from `cyan[2]`. Dark mode. */
export const dark = '#11252d80';
