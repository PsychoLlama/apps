/**
 * The observability worker. Spawned on the main thread by the OPFS log
 * backend (see `../logging/backends/opfs-worker.ts`). Does nothing yet — this
 * is the bare entrypoint that later work will build the off-main-thread
 * telemetry pipeline onto.
 */

export {};
