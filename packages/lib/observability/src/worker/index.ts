/**
 * The observability worker. Spawned from the main thread by `#spawn`
 * (see `../spawn.browser.ts`). Does nothing yet — this is the bare
 * entrypoint that later work will build the off-main-thread telemetry
 * pipeline onto.
 */

export {};
