## Purpose

Base component for floating elements. Internal only.

## Requirements

- CSS first. Positioning works without JavaScript.
- Progressively enhanced via `@floating-ui/dom`.
- Enhancements are opt-in. If a component doesn't need a runtime adjustment, it isn't included.
- Where pragmatic, enhancements are CSS context, not rules. Downstream components apply constraints.
