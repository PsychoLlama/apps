$schema: 'https://moonrepo.dev/schemas/project.json'

project:
  name: '@app/main'
  description: 'SolidStart host app'

dependsOn:
  - '@app/studio'
  - '@dev/storybook'
  - '@lib/design'
  - '@lib/shell'
  - '@lib/state'
  - '@lib/ui'

tasks:
  dev:
    command: 'pnpm'
    args: ['dev']
    local: true

  build-app:
    command: 'pnpm'
    args: ['build']
    inputs:
      - 'src/**/*'
      - 'vite.config.ts'
      - 'vite-ignored.ts'
      - 'wrangler.toml'
      - '/packages/app/studio/src/**/*'
      - '/packages/lib/**/src/**/*'
    outputs:
      - '.output'

  build:
    command: 'cp'
    args:
      - '-r'
      - '../../dev/storybook/storybook-static'
      - '.output/public/__storybook'
    deps:
      - '~:build-app'
      - '@dev/storybook:build'
    inputs:
      - '/packages/dev/storybook/storybook-static/**/*'
      - '.output/**/*'
    outputs:
      - '.output/public/__storybook'
