## Dev

- Export all rules in `index.ts`.
- 1 file per rule under `rules/`.
- Rule names must match file names.
- Don't make rules fixable without good reason.
- ESLint has caching enabled saved at `node_modules/.cache/eslint/`.
