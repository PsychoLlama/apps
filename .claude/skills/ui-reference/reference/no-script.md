# NoScript

Content shown only when scripting is unavailable. Renders a `<noscript>`.

Children render on the server only. Event handlers and reactive expressions inside are inert.

## Props

- `children`: Fallback markup for readers with scripting disabled.
